[phases.setup]
nixPkgs = ["...", "python311", "python311Packages.pip"]

[phases.install]
cmds = ["npm install", "pip install --break-system-packages -r requirements.txt"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start --workspace backend"
